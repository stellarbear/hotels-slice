import {Route, Outlet, Navigate} from "react-router";
import {RoutesBoundary} from "./RoutesBoundary";
import {RouteLazy} from "./RouteLazy";

export const Routing = {
  Route,
  Outlet,
  Navigate,
  RouteLazy,
  Routes: RoutesBoundary,

  
};
