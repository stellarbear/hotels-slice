import styled from "styled-components";

const PresetCenter = styled.div`
    display: grid;
    height: 100%;
    align-items: center;
    justify-content: center;
`;

export const presets = {
    center: PresetCenter,
};
