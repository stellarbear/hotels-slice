{
    "name": "@app/extensions-sentry",
    "version": "0.1.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist/"
    ],
    "description": "UI",
    "scripts": {
        "clean": "yarn build:clean",
        "watch": "yarn build:watch",
        "build": "yarn build:clean && tsc",
        "build:clean": "rimraf dist tsconfig.tsbuildinfo",
        "build:watch": "yarn build --watch"
    },
    "private": true,
    "sideEffects": false,
    "license": "MIT",
    "dependencies": {
        "@sentry/react": "^7.60.1"
    }
}
