import {QueryAsync} from "./QueryAsync";
import {QueryAwait} from "./QueryAwait";
import {QueryOverlay} from "./QueryOverlay";

export * from "./styles";
export const Query = {
    Async: QueryAsync,
    Await: QueryAwait,
    Overlay: QueryOverlay,
};
