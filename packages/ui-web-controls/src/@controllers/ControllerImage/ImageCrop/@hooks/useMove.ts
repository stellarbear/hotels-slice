import {useDomEvent} from "@app/extensions-react";
import * as React from "react";
import {ImageCropPoint} from "../@helpers";

export const useMove = (ref: React.MutableRefObject<HTMLDivElement | null>) => {
    
};
