import {useDomEvent} from "@app/extensions-react";
import * as React from "react";
import {between, getDistanceBetweenPoints} from "../@helpers";

export const useZoom = (ref: React.MutableRefObject<HTMLDivElement | null>) => {
    
};
